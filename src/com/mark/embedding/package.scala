package com.mark

/** Given a set of learners and a similarity function between
  * them, provides an approximate set of points in R^n that
  * preserves the distances between each other.
  */
package object embedding {
}